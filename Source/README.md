# CFAST Source Code

The CFAST and Cdata Fortran source code and CEdit Visual Basic code are included in the folders. The main controlling program is `cfast.f90` for CFAST and `CEdit.vb` for CEdit.  The `makefile` for compilation is in the `Build` folder.
